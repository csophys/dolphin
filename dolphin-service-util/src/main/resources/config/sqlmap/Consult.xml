<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.dolphin.dao.ConsultDao">

    <insert id="insert" parameterType="org.dolphin.entity.Consult" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Consult(AddTime, Code, Name, ParentId, SkillId, Rank, IconUrl, ChannelType)
        VALUES
        (
        now(),
        #{code},
        #{name},
        #{parentId},
        #{skillId},
        #{rank},
        #{iconUrl},
        #{channelType}
        );
        <selectKey resultType="int" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <update id="updateById">
        UPDATE Consult
        <set>
            <if test="consult.code!=null">Code=#{consult.code},</if>
            <if test="consult.name!=null">NAME=#{consult.name},</if>
            <if test="consult.parentId!=null">parentId=#{consult.parentId},</if>
            <if test="consult.skillId!=null">skillId=#{consult.skillId},</if>
            <if test="consult.rank!=null">rank=#{consult.rank},</if>
            <if test="consult.iconUrl!=null">iconUrl=#{consult.iconUrl},</if>
            <if test="consult.channelType!=null">channelType=#{consult.channelType}</if>
        </set>
        WHERE id=#{id}
    </update>
</mapper>